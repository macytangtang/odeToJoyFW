const getters = {
    navCollapse: state => state.navCollapse,
    userName: state => state.userName,
    userInfo: state => state.userInfo,
    addMeetingNum: state => state.addMeetingNum
}

export default getters
